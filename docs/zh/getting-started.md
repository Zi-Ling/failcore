# 快速开始

本指南展示 FailCore 如何在**执行时**阻止危险操作。

示例演示了一个常见的 agent 失败场景：

> 模型生成的工具调用删除了错误的目录。

---

## 安装

```bash
pip install failcore
```

---

## 问题：路径幻觉

考虑一个简单的删除目录的工具：

```python
import shutil

def delete_path(path: str):
    shutil.rmtree(path)
```

AI agent 可能*想要*删除一个临时文件夹，但产生了错误的路径：

```text
"/project"
```

没有保护的情况下，这会立即执行 — 并且是破坏性的。

---

## 使用 FailCore 保护操作

使用 FailCore 会话和策略包装执行：

```python
from failcore import run, guard

with run(policy="fs_safe", strict=True) as ctx:
    @guard
    def delete_path(path: str):
        import shutil
        shutil.rmtree(path)
    
    delete_path("/project")
```

**替代风格**（显式工具注册）：

```python
from failcore import run

def delete_path(path: str):
    import shutil
    shutil.rmtree(path)

with run(policy="fs_safe", strict=True) as ctx:
    ctx.tool(delete_path)
    ctx.call("delete_path", path="/project")
```

---

## 会发生什么

FailCore 会阻止删除操作，而不是执行它：

- ❌ 执行被拒绝
- 📛 引发违规异常
- 🧾 证据记录在 trace 中

FailCore 在操作**运行之前**进行验证。

---

## 为什么这很重要

- 模型的推理可能看起来正确
- 计划可能在逻辑上合理
- 但执行时的值仍然可能是错误的

FailCore 强制执行**最后一道防线保证**。

---

## 您将获得什么

当发生违规时，FailCore 提供：

- 确切的工具调用和参数
- 违反的策略规则
- 用于审计或重放的结构化 trace

这使得失败可解释 — 而不是神秘的。

---

## 下一步

- 探索不同的策略预设（`fs_safe`、`net_safe`、`safe`）
- 检查执行 trace
- 使用 `failcore policy init` 和 `failcore policy explain` 管理策略
- 将 FailCore 集成到 agent 运行时或代理中

有关更深入的设计细节，请参阅架构文档。
